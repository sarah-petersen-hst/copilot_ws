import React from 'react';

/**
 * Simple in-memory router for two-page navigation (Find Events, Saved Events).
 * @param {Object} props
 * @param {string} props.page - Current page key.
 * @param {function(string):void} props.setPage - Setter for current page.
 * @returns {JSX.Element}
 */
function SimpleRouter({ page, setPage }) {
  // This is a placeholder. Actual routing is handled in App.jsx.
  return null;
}

export default SimpleRouter;
